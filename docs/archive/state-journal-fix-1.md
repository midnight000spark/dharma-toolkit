# STATE — архив: хроника пакета FIX-1 (read-only)

> Детали пакета FIX-1 «Воспроизводимость + целостность данных» (2026-09-19…20,
> Qoder). Перенесено из горячего `STATE.md` §Журнал изменений ради бюджета
> hot-слоя (D-38): в горячем STATE остаются компактные итоговые строки.
> Не редактировать: правки пакета — amendment'ом с датой (I-7).
> Хроника фазы 7 — `state-journal-phases-7.md`.

| Дата | Изменение | Автор |
|------|-----------|-------|
| 2026-09-19 | **FIX-1/блок 1 (repro-restore, C10)**: дрейф `pubspec.lock` измерен и откачен — 38 версий (в т.ч. `drift 2.34.3→2.35.0`, `drift_dev 2.34.5→2.35.0`, `analyzer 13.3.0→14.4.0`, `package_config 2.2.0→3.0.0`) + два новых пакета (`process 5.0.6`, `yaml_edit 2.2.4`). Форма: `git checkout -- pubspec.lock` → `flutter pub get` («Got dependencies!», exit 0) → `git diff --exit-code -- pubspec.lock` = 0, то есть откат не перезарезолвился (`sdks: dart ≥3.13.1 <4.0.0, flutter ≥3.44.0` при 3.13.4/3.47.5). Обновление стека — отдельный пакет, здесь не делалось. `.gitignore` с осознанным правилом `docs/reference/*.pdf` закоммичен, не откачен. Итог: `git status --porcelain` пуст, analyze 0, 501/501. Коммит `4142c97` (1 файл, +3). **[ФАКТ] Расхождение условия пакета с деревом**: блок 4.5 (`fix-r27`) к моменту старта уже выполнен (`59e3f9f`, закрытие R-27 — `e99a2e9`), базовое число тестов — 501, а не 499, как в условии | Команда |
| 2026-09-19 | **FIX-1/блок 2 (migration-infra, W6/W7/W26)**: (W6) цепочка версий в `onUpgrade` обёрнута в `transaction(...)` — отказ посередине больше не оставляет частично новую схему при старом `user_version`, из-за которого `LazyDatabase` пере-брасывал ошибку открытия на каждом `ensureOpen` («база не откроется никогда'); (W7) `PRAGMA foreign_keys = ON` переехал в `migration.beforeOpen` и действует на любом исполнителе — `forTesting(NativeDatabase.memory())` без `setup:` раньше жил с FK OFF и каскад B-11 в тестах молча не срабатывал; дубль `setup: enableForeignKeys` оставлен намеренно (он включает FK до миграций, хук drift вызывается после них — порядок подтверждён по исходнику `drift 2.34.3`, MIT); (фикстура) рукописный `_AppDatabaseV2Raw` удалён, схема v2 снималась прогоном ревизии `f0b6c5e` (`git archive` → её же закоммиченный `.g.dart` → `AppDatabase.forTesting(NativeDatabase.memory())` → `sqlite_master`) и лежит в `test/fixtures/schema_v2_drift.sql`, класс переименован в `_AppDatabaseV2`; (W26) `docs/MIGRATIONS.md` — чек-лист ветки, таблица тестов, «не покрыто» сведено с кодом. **[ФАКТ] Опровергнуто условие блока 2.3**: `drift_schemas/` в дереве не существует (ни в `git ls-files`, ни на диске), а снимок формата `_meta.version 1.3.0` хранит модель, не DDL; публичного читателя в пине нет (F-70) — поэтому DDL получается прогоном нужной ревизии, а не чтением снимка. **[ФАКТ] Единицы времени**: `currentDateAndTime` — секунды, не миллисекунды (F-68), «починка ×1000» была бы регрессией. 3 мутации красные (снятая транзакция; снятый PRAGMA; `* 1000` в снимке). Коммит `bc89d3a` (+390/−57), 505 тестов. СТОП-1: отчёт принят, продолжено | Команда |
| 2026-09-20 | **FIX-1/блок 3 (миграционная целостность, C4 → D-45)**: **C4 опровергнута**. Ветка v5 (дедуп + слияние + `merge_conflict` + бэкфилл) не введена: популяция `preset_id IS NOT NULL AND preset_practice_id IS NULL` не порождается кодом — в `lib/` в `practices` пишут два места, материализация (`5805f5d`) пишет обе колонки с первого коммита и появилась в 5.0.2 вместе с колонкой (`20fac2c`), `PracticeRepository.create` (`21ef531`) не пишет `preset_practice_id` вовсе, а его единственный вызывающий (`796e5c8`) не передаёт `presetId`; на v2 (`f0b6c5e`) в `lib/features/tracker/` был только `tracker_tables.dart`. Эмпирика с живого устройства (`scripts/emu.sh up`, `adb root`, `adb pull app_flutter/dharma_toolkit.sqlite`): `user_version = 4`, `quick_check = ok`, 4 материализованные строки, **0** в популяции C4. Вместо миграции — инвариант на слое записи (`create` бросает `ArgumentError` на `presetId != null`) + тест-страж популяции + поведенческие тесты (`applyPreset` дважды поверх мигрированной базы; отказ бэкфилла по имени: кастомная «Простирания» сохраняет счёт 21 000, иначе пресетная строка унаследовала бы её). Бонусом закрыт второй прогон из чек-листа W6 (`_AppDatabaseNoMigration` откатывает `user_version` без изменения схемы). **[ФАКТ]** двойная доставка ошибки opener'а (`LazyDatabase`) наблюдалась вживую при отладке тестов этого блока и блока 4.5 — F-69. 3 мутации красные. Коммит `a2ab3d8` (+474/−7), 511 тестов. Решение владельца: вариант II (инвариант вместо миграции), эмулятор поднят для живой проверки | Команда |
| 2026-09-20 | **FIX-1/блок 4 (startup-surface, C1(1)/C2/C3/S12-min)**: (C1(1)) `PresetManager.init` и `ConfigModule.init` перестали быть all-or-nothing — битый blob в `presets.data` больше не уводит на экран с единственным механизмом «стереть всё»: снимается только ключ `active_preset_id`, данные не тронуты (D-26), старт продолжается «активной традиции нет»; в `ConfigModule` отказ накапливается пофайлово (как в `EventPackLoader`/`ContentPackLoader`), битый манифест остаётся фатальным. Молчаливая деградация закрыта классами: `FailureKind` + `subject` + интерфейс `ReportsOwnFailures`, `bootstrapModules` складывает доклады в `StartupOutcome.degradedModules`. (C2) `humanErrorWidget` обёрнут в `Directionality` — иначе `Text` без предка падает сам и фреймворк подставляет замену для замены. (C3) контроль цикла старта вынесен в `runStartupCycle` + `MountedRoot` (тестировать `main` = `runApp` всего приложения): отказ `bootstrap` и бросок из `mountApp` монтируют восстановление принудительно, условие зон-обработчика — «в корне нет рабочего приложения», а не «корень смонтирован»; у `_run` появился `catch` с видимым сообщением и счётчиком попыток; `disposeAll` в `bootstrapServices` больше не отменяет пересоздание реестра. (S12-min) экран показывает ограниченный режим (~40 строк). 3 мутации красные (`Expected: true / Actual: <false>`; `No Directionality widget found`; `Bad state: второй прогон тоже упал` наружу). Коммит `56744df` (+823/−55), 528 тестов | Команда |
| 2026-09-20 | **FIX-1/блок 4.5 (перепроверка R-27)**: форма — лог в файл, exit-код без конвейеров (F-63). Красного не поймано: полные прогоны ×5 (`+528: All tests passed!`, exit 0), изоляция recovery-файла зелёная, стресс — изоляции ×3, `--concurrency=1` (1:58) и `--concurrency=8` (0:27), всё exit 0. Итог по факту: **R-27 остаётся закрытой** (её причина — тайминговый артефакт — устранена `59e3f9f`, воспроизведения нет). Собственный разбор границы дал новое: `LazyDatabase` пере-брасывает ошибку opener'а дважды, второй раз — необработанной в зону (**F-69**); это второй, независимый от R-27 механизм «красный в полном прогоне / зелёный в изоляции», и он закрывается моком канала `path_provider` в тестах, затрагивающих прод-конструктор базы. Детерминированная мутация стража R-27 (`onReset` обгоняет незавершённое стирание) красная: `Expected: <0> / Actual: <1>` — та же ассерция, что в историческом красном (`Expected: <1> / Actual: <0>`), но с обратным знаком | Команда |
| 2026-09-20 | **FIX-1/блок 5 (recovery-wipe, C1(2)/(3))**: `DatabaseModule.init` получил активный зонд `PRAGMA quick_check` — «критичный модуль database» перестал быть немощным: повреждение файла попадает в `fatalFailures`, а не всплывает текстом на экране списка (сценарий (в) C1). Отказ зонда закрывает соединение, обнуляет инстанс и уходит наружу; цена — обход страниц на старте, вопрос таймаута и выноса зонда из критического пути оставлен за W19. `wipeLocalState` возвращает `WipeReport` (переименованный путь, ошибки, признак чистоты) вместо `void` c `debugPrint`-проглатыванием, закрывает соединения ДО файловых операций, сносит сайдкары `-journal/-wal/-shm` и переименовывает основной файл в `dharma_toolkit.sqlite.corrupt-<ts>` вместо удаления (шанс вытащить счёт — дух D-26); неполное стирание видно на экране, детали — в логе (B-19). **[ФАКТ] Три теста держали вакуумный контракт** «init не трогает платформу» (`database_module_test` ×2, `registry_integration_test` ×1) и покраснели от зонда; исправлены моком канала, а не ослаблением гарда, и две тавтологии (`expect(module.database, isNotNull)` на non-nullable) заменены поведенческими ассертами (файл существует сразу после init; `user_version` проставлен). 4 мутации красные (снятый зонд ×2 теста; проглоченный отказ close; убранные сайдкары; `delete` вместо `rename`). Коммит `286df5f` (+408/−21), 533 теста | Команда |
| 2026-09-20 | **FIX-1/блок 6 (docs + пуш)**: (а) этот архив + компактные строки в горячем STATE; (б) **D-45** заведено телом в `docs/reference/decisions.md#d-45` + индекс-строка; (в) новые факты **F-68** (единицы DateTime в drift), **F-69** (двойная доставка ошибки opener'а), **F-70** (снимок схемы — модель, публичного DDL-читателя в пине нет); (г) **БФТ v1.15** (файл-на-версию): §13 — R-27 закрыта и перепроверена, NFR-TEST-1 → 533, §15 — запись 1.15; указатели переведены на v1.15 в CONTEXT/README/docs-README; (д) счётчики 501 → 533 в freshness-строках STATE/CONTEXT/AGENT и в README/CONTEXT — с явной пометкой, что `flutter build apk` и `emu-fire` на этом дереве **не переверены** (замыкает наблюдение «цепочка сертификатов отстаёт от дерева»); (е) **ROADMAP не тронут** (план не менялся); (ж) мутационный payload вычищен: `grep -r MUTATION lib test` — пусто. Мутаций за пакет — 14 (блоки 2/3/4 по 3, R-27 — 1, блок 5 — 4), каждая красная цитата в этом архиве и в телах коммитов. Приёмка: analyze --fatal-infos 0, `flutter test` 533/533 (лог в файл), `./scripts/check_docs.sh` PASSED. Notify ✅/👀. Пуш формой F-63, CI — `./scripts/ci_status.sh <ref> --steps` до GREEN опросом; теги не ставятся (фикс-пакет) | Команда |

---

## Приёмка fix1-accept (2026-09-21) — живой прогон на устройстве

> Отдельный пакет, дописан append'ом; таблица FIX-1 выше не редактируется (I-7).
> Дерево: `1336c9d` (main, после принятия FIX-1). Устройство: AVD `dharma35`,
> API 35 / Android 15, x86_64, `Europe/Moscow`, release-APK.

| Дата | Изменение | Автор |
|------|-----------|-------|
| 2026-09-21 | **блок 0 (рельсы)**: `git status --porcelain` — пусто на `1336c9d`; `flutter analyze --fatal-infos` → exit 0 (`No issues found! (ran in 8.8s)`); `./scripts/check_docs.sh` → exit 0 (`PASSED (42 files, 626 refs)`); `scripts/emu.sh status` → `BOOT_OK`; теги: `fix-1` ещё не проставлен. Разрешение владельца (разовое, из условия пакета): пуш формой F-63 + тег `fix-1` на `1336c9d` + финальные docs-коммиты | Команда |
| 2026-09-21 | **блок 1 (сборка + плановое напоминание)**: первая попытка `flutter build apk --debug` — **exit 1 (28 s)**, `--release` — exit 1 (4 s): `LicenceNotAcceptedException: ndk;28.2.13676358`, `Using Android SDK: /usr/lib/android-sdk`. Корень — окружение, не дерево: `android/local.properties` (gitignored, автогенерат) указывал на системный SDK без NDK с принятыми лицензиями; `ANDROID_HOME` пуст. Починка: `flutter config --android-sdk /home/midnight/software/android_sdk`, `flutter config --jdk-dir /usr/lib/jvm/java-17-openjdk-amd64`. Повтор: `debug exit=0 (132s) \| release exit=0 (164s)`, `✓ Built build/app/outputs/flutter-apk/app-debug.apk`, `✓ Built ...app-release.apk (61.0MB)`; артефакты 214 175 218 Б и 61 028 986 Б. Установка: `adb uninstall dev.dharmatoolkit.dharma_toolkit` → `Success`; `adb install -r -g build/…/app-release.apk` → `Success` (2,4 s). Гранты: `emu-perms.sh grant` → `POST_NOTIFICATIONS -> granted=true`, `appops get SCHEDULE_EXACT_ALARM -> Uid mode: SCHEDULE_EXACT_ALARM: allow`. Старт → `/pick` собран из ассет-пресетов (в release `DevSeeder` выключен — F-65): «Выберите традицию», кликабельны «Тхеравада» и «Ньингма», `zen_soto`/`zen_rinzai`/`kagyu` — `enabled="false"` «Скоро — пресет ещё не готов». Тап «Ньингма» → экран «Практики» с 4 трекерами (Простирания / Мантра Ваджрасаттвы / Подношение мандал / Гуру-йога, «0 повторений», «0%») — материализация пресета в release работает. `./scripts/emu-verify.sh --expect-exact` → exit 0: `Записей по пакету: 5 (exact: 5, inexact: 0)`, `Несовпадений времени 08:00: 0`, записи `2026-09-21, 10-05, 10-21, 11-04, 11-19 08:00:00.000 window=0`. План вычитан из `shared_prefs/scheduled_notifications.xml` (5 записей, ids 100000…100004, заголовки «10-й день тибетского месяца»/«25-й день тибетского месяца», тексты «Полусамоцветный день (лунный день 10/25), календарь Пхугпа»). `./scripts/emu-fire.sh --expect-title "10-й день тибетского месяца"` → exit 0: `Перевод часов: 2026-09-21 07:59:00 (moment - 60s)` → `--- показ состоялся на 08:00:03 (id=100000) ---`, `pkg=dev.dharmatoolkit.dharma_toolkit … channel=dharma_reminders`, `Заголовок: 10-й день тибетского месяца`, `Текст: Полусамоцветный день (лунный день 10), календарь Пхугпа`, `emu-fire: часы возвращены → Mon Sep 21 00:52:13 MSK 2026`. Расхождение с условием пакета («4 exact-pending»): их 5, потому что горизонт плана скользящий (F-65, amendment 2026-09-21); признак приёмки (все exact, время 08:00) соблюдён. Побочное наблюдение среды: сразу после старта эмулятора SystemUI выдал ANR («System UI isn't responding») — лечится тапом Wait, к приложению не относится | Команда |
| 2026-09-21 | **блок 2 (wipe вживую)**: до — `ls -la app_flutter/` = `dharma_toolkit.sqlite` 40 960 Б, md5 `52c55b2986293124bd54a843c90155e2`, `pragma user_version` = 4, заголовок `SQLite format 3\0`. Порча: `dd of=… bs=1 count=64 conv=notrunc` 64 байт `CORRUPT-…` в заголовок (без усечения размера). Первый же прогон показал ловушку метода: подложенные `-wal`/`-shm` исчезли **до** стирания — их снял SQLite при закрытии неудачного открытия, то есть удаление сайдкаров было бы приписано не нашему коду. Повтор с пересадкой сайдкаров уже после выхода на экран восстановления (`-journal` 0 Б, `-wal`/`-shm` по 10 Б, владелец `u0_a214`, режим 600). Рестарт на битой базе → **не чёрный экран**: `Не удалось прочитать данные` + `Попробуйте ещё раз. Если не поможет — сбросьте настройки.` + `Приложение работает в ограниченном режиме:` + `активная традиция не прочитана — preset_manager: nyingma` (S12-min + C1(1) вживую) + кнопки `Попробовать снова`/`Сбросить настройки`. Лог: `Зонд целостности БД не прошёл: SqliteException(26): while selecting from statement, file is not a database, file is not a database (code 26)` / `Causing statement: PRAGMA user_version;` со стеком `database_module.dart:61 DatabaseModule.init` → `module_registry.dart:117` → `startup.dart:50 bootstrapModules` → `main.dart:228`; деградация: `Модуль "preset_manager" не инициализирован (nyingma): Bad state: DatabaseModule not initialized (активная традиция не прочитана)`. Тап «Сбросить настройки» → диалог `Сбросить все данные?` / `Будут полностью удалены настройки и история практик на этом устройстве. Отменить это будет невозможно.` → тап «Сбросить» → в логе `recovery: файл базы сохранён как /data/user/0/…/app_flutter/dharma_toolkit.sqlite.corrupt-1789941700551`; после — в каталоге только `dharma_toolkit.sqlite` (40 960 Б, md5 `60099b25e27f4a64b2a99e910b2420b8`, заголовок `SQLite format 3\0`, `user_version` = 4) и `.corrupt-1789941700551`; **все три сайдкара сняты**. Рестарт → чистый `/pick`. Цикл повторён вторично (порча → wipe): появился второй снимок `.corrupt-1789941700551`/`.corrupt-1789941936505` с разными метками времени, `rename` не затирает предыдущий случай. После второго стирания — снова чистый `/pick`; `adb unroot` возвращён | Команда |
| 2026-09-21 | **блок 2 (новая находка B-25)**: в том же сценарии два тапа «Попробовать снова» на заведомо неисчезающем отказе. `logcat` показывает три прогона зонда в одном pid (`4612`) — попытки были; на экране строки `Повторная попытка не удалась (попытка N)` нет. Причина по коду: `retry-failure` рендерится только когда `widget.onRetry` бросает, а production-`onRetry: start` → `runStartupCycle`, который по своему контракту отказ наружу не выпускает (монтирует восстановление и возвращает `MountedRoot.recovery`, `startup.dart:114-118`); тест гарда подаёт `onRetry` с `failNext`, то есть проверяет виджет, а не связку. Зарегистрировано как **B-25** (открыт, приоритет средний); внутри приёмочного пакета не чинится (стоп-гейт «находку не чинить молча»). Это и есть тот внешний прогон, который amendment к **D-42** делает обязательным | Команда |
| 2026-09-21 | **блок 3 (документы, тег, уборка)**: freshness STATE/CONTEXT/AGENT переведён с оговорки «сборка и устройство не переверены» на живой прогон этого пакета; строки журнала (горячие — компактно, здесь — полностью); тело **B-25** в STATE; строка чек-листа «double-run цепочки на копии живой БД устройства» в `docs/MIGRATIONS.md`; amendment к **D-42** (роутинг qwen3.8-flash/Qoder, предел самоотчёта модели, обязательный внешний прогон) и два amendment к **F-65** (окружение сборки; скользящий горизонт плана); тег `fix-1` на `1336c9d` и пуш формой F-63; `.tmp/` приёмки (снимок устройства `device-db/`, `probe/`, архив фикстуры `f0b6c5e`, черновые хелперы) снесён, скрипты `scripts/` не изменялись | Команда |
