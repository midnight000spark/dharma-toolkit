# Дхарма-тулкит — Архитектурная конституция

**Версия 2.12** · 2026-09-01 · пересмотрена по итогам внешнего аудита (`docs/REVIEW-2026-08-31.md`); v2.8 — процессные правила гигиена/свежести (решение пользователя 2026-09-01); v2.9 — гигиена кластера переформулирована косвенными ссылками + правило свежих сессий (пакет A-resume); v2.10 — указатель на БФТ обновлён v1.5 → v1.6 (пакет 5b.5); v2.11 — правило приёмки «зелёный CI подтверждается командой» (D-33, R-25); v2.12 — политика TODO, теги контрольных точек, гигиена скриптов (пакет 5b.6)

## Миссия
Создать универсальное мобильное приложение для буддийских практикующих, объединяющее календарь, счетчики практик и контент под разные традиции.

## Иерархия документов (приоритет источников)

**БФТ → ROADMAP → STATE → AGENT → код-комментарии**

- **БФТ** (`docs/BFT-v1.7.md`, актуальная версия — файл с наибольшим номером) — что строим и для кого. Источник требований.
- **ROADMAP.md** — куда идём, порядок этапов. Меняется только с одобрения оркестратора.
- **STATE.md** — где мы сейчас: статусы, факты, решения, угрозы, баги.
- **AGENT.md** (этот файл) — как работаем: принципы и правила приёмки.
- **Комментарии в коде** — lowest priority, не источник истины о проекте.

**Правило согласования (урок 4 аудита, R-17):** при любом конфликте
между документом и кодом **главный источник правды о факте — код**.
Документ, расходящийся с кодом, — это баг документа: сначала правим
документ, потом работаем. Отметка «сделано» в документе обязана
подтверждаться командой проверки (см. «Правила приёмки»).

## Архитектурные принципы

### 1. Модульность (Plugin Architecture)
- Ядро не знает о конкретных школах/традициях
- Каждый модуль — независимый плагин с четким интерфейсом
- Модули общаются только через event bus или dependency injection
- Запрещены прямые импорты между модулями
- **Единственное ослабление (D-20):** точка агрегации схемы БД —
  `lib/core/db/app_database.dart`. Ядро объявляет `@DriftDatabase(tables: [...])`
  со таблицами всех фич, потому что Drift при AOT не поддерживает динамическую
  регистрацию таблиц, а единый миграционный трек важнее изоляции слоя данных.
  Ослабление **зафиксировано и не расширяется**: остальное ядро (events, config,
  module) импортировать features не вправе; guard-тест проверяет оба направления
  (core→features, кроме `core/db`, и feature→feature).

### 2. Конфигурируемость через пресеты
- Школы/традиции = JSON-файлы (пресеты), не хардкод
- Пресет описывает какие модули включить и с какими настройками
- Пользователь может создавать свои пресеты
- Применение пресета **обязано материализовывать данные** (создавать трекеры
  из `preset.practices` идемпотентно) — пресет, который «загружен, но ничего
  не создаёт», не считается работающим (B-3).

### 3. Изоляция данных
- У каждой традиции свои данные в БД (с префиксами/тэгами)
- Общие модули (календарь, счетчик) работают с данными абстрактно
- Возможность иметь несколько активных традиций одновременно
- `traditionTag` берётся из активного пресета, а не из литерала в коде (B-4)

### 4. Dependency Injection (D-4 + D-22)
- **Riverpod — единственная система DI.** Service locator (`ModuleRegistry.instance.get('...')`
  со строковым ключом и приведением типа в рантайме) запрещён — это паттерн
  get_it, от которого отказались.
- Зависимости в UI-слой и репозитории приходят через провайдеры
  (например, провайдер `AppDatabase` с оверрайдом в `ProviderScope`).
- Глобальные переменные для состояния запрещены.
- Модули, которым нужна БД, получают её через инъекцию (`QueryExecutor` /
  экземпляр), а не создают сами (R-13).

### 5. Миграции БД (D-17)
- `onUpgrade` применяет версии **по одной**; каждая версия схемы обязана иметь
  свою ветку; неизвестная версия бросает `StateError` при открытии базы
  (падает у разработчика, а не молчит у пользователя).
- Новые таблицы — `m.createTable`, новые колонки — `m.addColumn`;
  `createAll()` в ветках миграций **запрещён**.
- `stepByStep` с дампами схемы — целевая форма; переход на неё выполняется
  заменой тела `onUpgrade` и не ломает существующие ветки (процедура: `docs/MIGRATIONS.md`).
- Каждое изменение схемы сопровождается тестом миграции, проверяющим
  **сохранность данных** при переходе версий.

### 6. EventBus (D-7, отложен по D-21)
- Event bus — низкоуровневый транспорт, не состояние UI.
- **D-21: шина отложена до реального потребителя (Этап 6).** В Этапе 5
  междомодульные коммуникации идут через Riverpod-провайдеры и Drift-стримы.
- До подключения шина обязана быть инстанцируемой (тестируемой локально,
  без необратимого `dispose` глобального синглтона) — R-12.

## Технологический стек (подтвержден)
- **Framework**: Flutter 3.47.1 (stable), Dart 3.13.1
- **State Management**: Riverpod (flutter_riverpod 2.6.1) — state + DI (D-4)
- **Local DB**: Drift (фактически 2.34.3, F-24 — документацию сверять с pubspec.lock)
- **Routing**: go_router 18.0.0
- **Testing**: flutter_test
- **Локализация**: flutter_localizations (SDK) + intl 0.20.3 (pubspec.lock, F-24), только `ru` — подключено в 5.0.5 (R-15, D-28)

## Структура проекта
```
lib/
  core/
    module/           # Plugin Manager, Module Interface
    events/           # Event Bus (отложен, D-21)
    config/           # Config Loader, Preset Manager
    db/               # Drift setup, миграции (D-20: точка агрегации схемы)
  features/
    calendar/         # Calendar Module (абстрактный)
    tracker/          # Tracker Module (счетчики)
    content/          # Content Module (сутры, цитаты)
    onboarding/       # Онбординг и выбор традиции
  shared/
    widgets/          # Переиспользуемые UI компоненты
    utils/            # Общие утилиты
    theme/            # Темы и стили
    l10n/             # Локализация
test/
docs/                 # БФТ, ревью, MIGRATIONS.md
presets/              # JSON файлы пресетов
```

## Работа с ROADMAP.md

### Обязательный порядок чтения
1. **Перед каждым этапом** — перечитай ROADMAP.md для понимания контекста и цели этапа
2. **Перед каждым шагом** — перечитай STATE.md для понимания текущего статуса
3. **AGENT.md** — перечитывай только в начале этапа или при потере архитектурного контекста

### Правила изменения ROADMAP.md
- ROADMAP.md изменяется ТОЛЬКО с одобрения оркестратора (человека)
- Любое изменение фиксируется в секции "Changelog плана" с датой
- dharma не имеет права менять ROADMAP.md без явной инструкции

## Tier-0: Документация библиотек
Перед работой с любой внешней библиотекой/пакетом:
1. Прочитай официальную документацию (pub.dev / README / API docs)
2. Сверь API с текущей версией Flutter/Dart (deprecated — как в F-16)
3. Зафиксируй ключевые факты в STATE.md (версия, API, ограничения, лицензия)
4. Только после этого пиши код
5. Версии сверяй с `pubspec.lock`, а не с ограничениями `pubspec.yaml` (F-24)

## Правила приёмки (уроки аудита 2026-08-31)

### Урок 1. Зелёный тест — это утверждение о гарантии, а не гарантия
Guard-тест принимается только так: **внести нарушение руками и убедиться,
что тест покраснел.** Тест, который не мог упасть, не доказал ничего.
Заглушки `expect(true, isTrue)` запрещены.

### Урок 2. Критерий выхода этапа — демонстрируемый сценарий
Формулировка вида «применение пресета создаёт записи в БД» закрывается только
предъявляемым прогоном: *«на чистой установке выбираю Ньингма → появляются
практики нёндро → тапаю → счёт растёт → после перезапуска сохранился».*
Набор файлов/классов/зелёных юнит-тестов критерием не является.

### Урок 3. Чинить на слое причины
Диагноз бага обязан указывать слой, где живёт причина (домен, а не UI для B-2;
слой данных для B-1). Точечные UI-костыли, которые надо повторять в каждом
новом месте, запрещены.

### Урок 4. Тесты должны ломаться от неправильного поведения
Тест проверяет поведение (путь сохранения, гонку, границy), а не факт отрисовки
виджета. «Форма покрыта тестами» без покрытия пути сохранения — не покрыта.

### Урок 5. Гонки и атомарность — детерминированные тесты на уровне репозитория; виджет-тесты — только UX-проверки (F-38)

### Общее правило
Каждое «сделано/✅/☑️» в любом документе подтверждается командой проверки
или предъявляемым сценарием. Не подтверждено — статус снимается.

### Зелёный CI — тоже проверяемое утверждение (D-33, R-25)
«CI зелёный» подтверждается **командой**, а не взглядом в веб-UI:
`./scripts/ci_status.sh <ref>` (exit 0; `--steps` — кто именно положил сборку).
Скрипт читает публичный API GitHub без токена, потому что авторизованный
доступ может быть недоступен агенту (невалидный ключ `gh` — F-41/F-53).
Если статус получить не удалось (приватизация репо, анонимный лимит 60/ч) —
в отчёте и в STATE стоит ⏳ с причиной, а не ✅.

### Свежесть документов (v2.8)
- **TODO обязана быть актуальной:** ссылка в TODO на закрытый этап — баг
  документа (чинить/снимать сразу, R-17); TODO, пережившая свою причину,
  плодит ложные объёмы работ.
- **README актуален после тегирования этапа:** закрытие этапа тегом
  `stage-*` обязывает сверить блок «Статус» README.md с фактическим
  состоянием — README видит внешний человек, устаревший статус в нём
  искажает картину не меньше, чем ложная ✅ в STATE.
- **Политика TODO (v2.12):** отложенное намерение в код не пишется — оно
  живёт в STATE.md/ROADMAP.md с номером (H-/B-/R-/D-/этапный); ссылка
  «сделаем в Этапе N» допустима только как указатель на существующую
  запись-владелец. Удаление TODO без переноса намерения в реестр — баг
  документа (намерение теряет владельца и повторяется чужим пакетом).
- **Теги контрольных точек (v2.12):** принятый сабпакет получает
  `stage-X.Y` сразу по завершении приёмки своего пакета, тег этапа
  (`stage-X`) — только в конце этапа, после ручной приёмки. Тег,
  «выбившийся» из порядка (заведён раньше, чем принят, или забыт), —
  дыра в контуре приёмки: ссылка в docs обязана резолвиться в коммит,
  содержащий заявленные артефакты.

### Гигиена скриптов, правящих файлы (v2.12)
Инструментальные скрипты пакета (мутации, патчи, генерация) обязаны
**прочитать файл в память до открытия на запись**: `open(p,'w')` затирает
содержимое, и непрочитанное теряется безвозвратно (урок инцидента
5b.3-resume: скрипт мутации затёр lib-файл, восстановление из git).
Правка чужих рабочих файлов предпочтительна `git checkout --` для отката
вместо обратного «read-modify-write» в том же скрипте.

## Модельная стратегия (D-19)
- **Qwen3.7 Plus выведен из эксплуатации** (недельный лимит исчерпан,
  стоимость росла нелинейно с контекстом — F-30).
- Разработка — только новые сессии на **Qwen3.8 Flash (variant Default = xhigh)**.
- Тяжёлые операции (синхронизация docs, большие пакеты) — только в свежем
  контексте.
- **Каждый пакет — в свежей сессии** (v2.9, урок обрыва #12 из R-18): сессия
  завершённого (или оборвавшегося) пакета для следующего не переиспользуется;
  недоделанный пакет довершается новым пакетом в свежей сессии, а не
  продолжением умершей.
- Глубина размышления: **Default (= xhigh) — кодовые пакеты; medium —
  механические docs-работы; low не используется** (дока Qвена: на агентных
  задачах низкий effort даёт ретраи и суммарно дороже — F-39). После
  полноглубинных (xhigh) подпакетов обновлять сессию (preserve_thinking
  раздувает контекст).
- **Самоотчёт модели**: агент начинает каждую сессию с сообщения, на какой модели
  и variant'е он реально выполняется; расхождение с настоящей секцией — **СТОП**
  и отчёт оркестратору (урок инцидента F-37: конфиг агента молча противоречил D-19).
  Конфиг `.opencode/agent/dharma.md` закреплён за этой секцией: `model:
  opencode-go/qwen3.8-flash`, `variant: xhigh` (у этой модели нет high/max;
  невалидное значение schema пропускает молча и даёт Default — F-39).

## Нотификации (D-27)
Агент отправляет уведомления через скилл `telegram-notify` в трёх точках:
- Завершение пакета: `✅ <пакет>: <итог>, тесты N/N`
- Стоп-гейт: `🛑 СТОП <пакет>: <причина>`
- Ручная приёмка: `👀 <пакет>: зелёный, прогони сценарий: <сценарий>`
Вызов: `./scripts/notify.sh "<сообщение>"`.
Сообщения ≤200 символов, без кода и путей. Fail-silent: при отсутствии
конфига скрипт молча exit 0, пакет не падает. Токен вне репо
(`~/.config/dharma/telegram`, 600) — коммитить запрещено.

## Правила кода

### Запрещено
- Хардкодить названия школ в ядре и в навигации (traditionTag из пресета)
- Создавать циклические зависимости между модулями
- Писать код без тестов
- Использовать глобальные переменные для состояния
- Service locator вместо провайдеров (D-4/D-22)
- `createAll()` в ветках миграций (D-17)
- Заглушки тестов `expect(true, isTrue)` (урок 1)
- **Гигиена кластера (v2.8; косвенная формулировка — v2.9):** вводить новые
  упоминания терминов кластера из F-47/F-48 и традиции вне MVP-скоупа (D-23)
  в коде, комментариях, docs и payload коммитов/тестов. Существующие
  упоминания в файлах не чистятся (перечень терминов — реестр в H-4;
  инвентарь упоминаний — журнал STATE.md; граница «не чистить» — записи
  старше пакета A). Причина — страховка R-18 по решению пользователя
  2026-09-01: кластер формально не подтверждён как триггер (H-4 опровергнута
  15/15 зондов), но дешёвая перестраховка дешевле мёртвых сессий. Само
  правило обязано формулироваться только косвенными ссылками — прямое
  перечисление терминов в самом правиле противоречит ему (устранено
  в A-resume).

### Обязательно
- Каждому модулю — свой набор тестов
- Интерфейсы для всех внешних зависимостей
- Документация к публичным API
- Коммиты по Conventional Commits
- Обновление STATE.md каждый шаг; снятие статусов «сделано» при расхождении с кодом

### Коммиты
- `feat(module_name):` — новый функционал
- `fix(module_name):` — исправление бага
- `refactor(module_name):` — рефакторинг
- `test(module_name):` — добавление тестов
- `docs:` — документация
- `chore:` — инфраструктура

## Схема классификации информации (для STATE.md)

### [ГИПОТЕЗА] — H-{номер}
Предположения, требующие проверки.
```
- **H-{номер}**: {Описание}
  - Статус: [новая|тестируется|подтверждена|опровергнута]
  - Как проверить: {план проверки}
  - Результат: {заполняется после проверки}
```

### [ФАКТ] — F-{номер}
Подтвержденная информация.
```
- **F-{номер}**: {Описание}
  - Источник: {откуда известно}
  - Дата: {когда подтверждено}
```

### [РЕШЕНИЕ] — D-{номер}
Принятые архитектурные решения.
```
- **D-{номер}**: {Описание}
  - Причина: {почему это}
  - Альтернативы: {что еще рассматривали}
  - Дата: {когда принято}
```

### [УГРОЗА] — R-{номер}
Риски и проблемы.
```
- **R-{номер}**: {Описание}
  - Вероятность: [низкая|средняя|высокая]
  - Влияние: [низкое|среднее|критическое]
  - Митигация: {как бороться}
  - Статус: [открыта|в работе|закрыта]
```

## Вход в курс (свежая сессия)

Постоянное правило для каждой новой сессии агента. Порядок восстановления
контекста до первого действия:

1. **CONTEXT.md** — суть и статус проекта за 30 секунд.
2. **STATE.md** — факты, решения, баги, «Следующее действие».
3. **ROADMAP.md** — критерии текущего этапа.
4. **AGENT.md** — конституция и правила приёмки.
5. **Базовая линия** до первой правки: `flutter analyze` (0 проблем),
   `flutter test` (все зелёные), `git status` (чисто), `git tag -l`
   (ожидаемые теги на месте). Любое расхождение — СТОП и отчёт.
6. **Документ противоречит коду — код истина.** Расхождение фиксировать
   в STATE.md как [ФАКТ] или [УГРОЗА] R-17.

## История конституции
- **v1** (2026-08-26): исходная — миссия, три принципа модульности, стек,
  структура, Tier-0, правила кода, схема классификации.
- **v2** (2026-09-01, по итогам аудита `docs/REVIEW-2026-08-31.md`):
  - добавлена иерархия документов и правило «код — источник истины о факте» (R-17);
  - D-20: агрегация схемы БД в ядре как единственное ослабление принципа №1;
  - D-4 усилен D-22: service locator запрещён явно, DI только через Riverpod;
  - D-21: EventBus отложен до реального потребителя;
  - D-17: регламент миграций поднят в конституцию;
  - добавлены правила приёмки (уроки аудита 1–4);
  - добавлена модельная стратегия D-19;
- стек приведён в соответствие с pubspec.yaml (mocktail убран из «подтверждённого»,
  добавлена локали zация ru).
- **v2.1** (2026-09-01, D-27): добавлен скилл telegram-notify для односторонних
  уведомлений о статусе пакетов; fail-silent, токен вне репо.
- **v2.2** (2026-09-01): добавлена секция «Вход в курс (свежая сессия)» —
  постоянный порядок восстановления контекста и базовая линия до первой правки.
- **v2.3** (2026-09-01, инцидент F-37): в модельной стратегии — правило самоотчёта
  модели в начале каждой сессии; `.opencode/agent/dharma.md` исправлен
  (qwen3.7-plus → qwen3.8-flash, variant high) и синхронизирован с конституцией v2.2.
- **v2.4** (2026-09-01, F-39): модельная стратегия приведена к реальному меню
  variant'ов (Default/low/medium/xhigh; high/max у qwen3.8-flash нет, невалидное
  значение молча даёт Default); в правилах приёмки — урок 5 (урок F-38:
  гонки/атомарность детерминируются на уровне репозитория, виджет-тесты — UX).
- **v2.5** (2026-09-01, 5.0.6/R-17): в стеке mocktail переведён из «подтверждённого»
  в явное «не подключён» (в pubspec.yaml его нет, моков нет — расхождение документа
  с кодом устранено; версия v2.4 к тому моменту уже занята правкой F-39).
- **v2.6** (2026-09-01, 5b блок 0, R-17): указатель на БФТ исправлен с устаревшего
  `docs/BFT-v1.1.md` на актуальный `docs/BFT-v1.4.md` с правилом «актуальная версия —
  файл с наибольшим номером» (конвенция версионирования БФТ файлами роняла указатели —
  то же предупреждение продублировано в CONTEXT.md и `.opencode/agent/dharma.md`).
- **v2.7** (2026-09-01, sync-docs-5b): указатель на БФТ обновлён v1.4 → v1.5
  (FR-CAL-1/2 синхронизированы с кодом 5b.1/5b.2; §13 БФТ объявлен срезом при
  реестре STATE.md — устранён второй источник истины по багам/рискам); CONTEXT.md
  и `.opencode/agent/dharma.md` синхронизированы.
- **v2.8** (2026-09-01, пакет A по решению пользователя): процессные правила —
  (1) **гигиена кластера** в «Запрещено»: новых упоминаний кластера в коде/комментариях/
  docs/payload не вводить, существующие не чиститься (страховка R-18; H-4 опровергнута
  зондами 15/15, но перестраховка дешевле мёртвых сессий); (2) и (3) секция
  «Свежесть документов» в правилах приёмки: TODO со ссылкой на закрытый этап = баг
  документа; README сверяется после тегирования `stage-*`.
- **v2.9** (2026-09-01, пакет A-resume): самопроверка пакета A выявила, что правило
  гигиены (введено в v2.8) было записано прямыми упоминаниями кластера — строки
  переформулированы только косвенными ссылками новым коммитом (граница «не чистить
  существующие» относится к упоминаниям старше пакета A); «Модельная стратегия»
  дополнена правилом свежих сессий (урок обрыва #12, R-18). *Строка истории заведена
  постфактум в v2.11: правка была в репозитории, записи в этом списке не было;
  дублирующая строка без пометки удалена в v2.12 (баг документа, R-17).*
- **v2.10** (2026-09-01, 5b.5): указатель на БФТ обновлён v1.5 → v1.6 (файл-на-версию);
  *строка истории также постфактум*.
- **v2.11** (2026-09-01, CI-наведение порядка): в правилах приёмки — секция «Зелёный CI —
  тоже проверяемое утверждение» (D-33, R-25): статус подтверждается командой
  `./scripts/ci_status.sh` (анонимный публичный API, exit 0/1, `--steps`), при недоступности
  — ⏳ с причиной вместо ✅. Причина — F-53: ключ `gh` в keyring невалиден после отзыва PAT
  (F-41), и «CI зелёный» мог повиснуть как непроверяемое утверждение.
- **v2.12** (2026-09-01, пакет 5b.6): процессные правила приёмки — (1) **политика TODO**
  в «Свежести документов»: отложенное намерение живёт в STATE/ROADMAP с номером,
  а не в виде TODO в коде; удаление TODO без переноса намерения = баг документа;
  (2) **теги контрольных точек**: сабпакет → `stage-X.Y` по завершении, тег этапа
  `stage-X` — только в конце после ручной приёмки (урок «выбившихся тегов»);
  (3) новая секция **«Гигиена скриптов, правящих файлы»** — чтение в память до
  открытия на запись (урок инцидента 5b.3-resume: скрипт мутации затёр lib-файл).
  Попутно удалена дублирующая строка истории v2.9 (баг документа, R-17).
